name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: 136.116.204.63
          username: sahabchaudhary080
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd Riddhi
            git pull origin main
            npm install
            pm2 restart flower-app
